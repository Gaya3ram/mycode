#include<stdio.h>
int main()
{
int i,flag,min,max;
printf("enter the two intervels: ", min,max);
scanf("%d %d",&min,&max);
printf("prime number between % and & are, min,max);
while(min<max)
flag=0;
for(i=0;i<low/2;++i)
{
if(min%2==0)
flag==0;
break;
}
if(flag==0)
printf("%d",min);
++low;
}
return 0;
}

